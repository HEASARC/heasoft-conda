--- a/tcltk/iwidgets/configure
+++ b/tcltk/iwidgets/configure
@@ -2027,6 +2027,8 @@ test -z "$INSTALL_DATA" && INSTALL_DATA='${INSTALL} -m 644'
 ITCL_LIB_DIR=""
 if test -r $itcl_SRC_DIR/library/itcl.tcl; then
     ITCL_LIB_DIR=$itcl_SRC_DIR/library
+elif test -r $with_itcl/itcl.tcl; then
+    ITCL_LIB_DIR=$with_itcl
 fi
 
 if test -z "$ITCL_LIB_DIR"; then
